---
const apps = [
	{
		id: "keepnbuild",
		name: "KeepNBuild",
		href: "/apps/keepnbuild",
		domain: "keepnbuild.com",
		tagline: "Custom Travel Design",
		lead: "당신만의 여정을 직접 그리세요.",
		desc: "어디로 떠날지, 무엇을 경험할지. 패키지 안에 없는 여행을 직접 설계하고 기록합니다. 경험이 쌓일수록 여행이 깊어집니다.",
	},
	{
		id: "worthee",
		name: "Worthee",
		href: "/apps/worthee",
		domain: "worthee.io",
		tagline: "Honor & Self-Management",
		lead: "꾸준함이 빛나는 순간.",
		desc: "매일의 실천을 기록하고, 과정을 인증하고, 커뮤니티가 함께 확인합니다. 혼자만의 다짐이 아닌, 함께 빛나는 꾸준함.",
	},
];
---

<section class="portfolio" id="portfolio" aria-labelledby="portfolio-heading">
	<div class="container">
		<p class="portfolio-eyebrow reveal" lang="en">Portfolio</p>
		<h2 id="portfolio-heading" class="portfolio-title reveal" style="--reveal-delay: 0.1s">
			우리가 만드는 것.
		</h2>

		<div class="portfolio-list">
			{apps.map((app, i) => (
				<a
					href={app.href}
					class="portfolio-item reveal"
					data-app={app.id}
					style={`--reveal-delay: ${(i + 1) * 0.15}s`}
				>
					<div class="portfolio-header">
						<h3 class="portfolio-name" lang="en">{app.name}</h3>
						<span class="portfolio-domain" lang="en">{app.domain}</span>
					</div>
					<p class="portfolio-tagline" lang="en">{app.tagline}</p>
					<p class="portfolio-lead">{app.lead}</p>
					<p class="portfolio-desc">{app.desc}</p>
				</a>
			))}
		</div>
	</div>
</section>

<style>
	.portfolio {
		position: relative;
		z-index: 1;
		padding-block: var(--ls-space-20);
		background: var(--ls-surface-raised);
		border-top: 1px solid var(--ls-border-subtle);
		border-bottom: 1px solid var(--ls-border-subtle);
	}

	.portfolio-eyebrow {
		font-family: var(--ls-font-ui);
		font-size: var(--ls-text-xs);
		font-weight: 600;
		color: var(--ls-text-muted);
		letter-spacing: var(--ls-tracking-widest);
		text-transform: uppercase;
		margin-bottom: var(--ls-space-4);
	}

	.portfolio-title {
		font-family: var(--ls-font-display);
		font-size: var(--ls-text-3xl);
		font-weight: 700;
		line-height: var(--ls-leading-tight);
		color: var(--ls-text);
		margin-bottom: var(--ls-space-12);
	}

	.portfolio-list {
		max-width: 860px;
	}

	.portfolio-item {
		display: block;
		padding: var(--ls-space-10) var(--ls-space-6);
		margin-inline: calc(-1 * var(--ls-space-6));
		border-top: 1px solid var(--ls-border-subtle);
		border-radius: var(--ls-r-lg);
		transition: background-color var(--ls-dur-fast) var(--ls-ease);
	}

	.portfolio-item:last-child {
		border-bottom: 1px solid var(--ls-border-subtle);
	}

	.portfolio-item:hover {
		background-color: var(--ls-surface);
	}

	.portfolio-header {
		display: flex;
		align-items: baseline;
		justify-content: space-between;
		margin-bottom: var(--ls-space-5);
	}

	.portfolio-name {
		font-family: var(--ls-font-display);
		font-size: var(--ls-text-3xl);
		font-weight: 700;
		color: var(--ls-text);
		letter-spacing: var(--ls-tracking-tight);
	}

	.portfolio-domain {
		font-family: var(--ls-font-ui);
		font-size: var(--ls-text-xs);
		font-weight: 500;
		color: var(--ls-text-muted);
		letter-spacing: var(--ls-tracking-wide);
	}

	.portfolio-tagline {
		font-family: var(--ls-font-ui);
		font-size: var(--ls-text-xs);
		font-weight: 500;
		color: var(--ls-text-muted);
		letter-spacing: var(--ls-tracking-wide);
		margin-bottom: var(--ls-space-5);
	}

	.portfolio-lead {
		font-family: var(--ls-font-display);
		font-size: var(--ls-text-xl);
		font-weight: 600;
		color: var(--ls-text);
		line-height: var(--ls-leading-snug);
		margin-bottom: var(--ls-space-4);
	}

	.portfolio-desc {
		font-family: var(--ls-font-story);
		font-size: var(--ls-text-base);
		color: var(--ls-text-secondary);
		line-height: var(--ls-leading-relaxed);
		max-width: 50ch;
	}

	@media (max-width: 768px) {
		.portfolio-header {
			flex-direction: column;
			gap: var(--ls-space-1);
		}
	}
</style>
