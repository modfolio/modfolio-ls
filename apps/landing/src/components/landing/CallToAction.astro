---
---

<section class="cta reveal" aria-label="시작하기">
	<div class="cta-wash" aria-hidden="true"></div>
	<div class="container cta-inner">
		<a href="/auth/login" class="btn-gradient" lang="en">Discover</a>
	</div>
</section>

<style>
	.cta {
		position: relative;
		z-index: 1;
		padding-block: var(--ls-space-20);
		min-height: 70vh;
		display: flex;
		align-items: center;
		justify-content: center;
		overflow: hidden;
	}

	.cta-wash {
		position: absolute;
		inset: 0;
		background: var(--ls-gradient-soft);
		pointer-events: none;
	}

	.cta-inner {
		position: relative;
		text-align: center;
		display: flex;
		flex-direction: column;
		align-items: center;
	}


</style>
